name: Python CI
on: push
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
        - name: install1
          run:  pip install poetry
        - name: install2
          run: make install
        - name: run-linter
          run:  make lint
